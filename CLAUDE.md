# CLAUDE.md

## Project Overview
אתר קטלוג לעסק בנישת חשפניות להזמנה (מסיבות רווקים וכו'). המטרה הסופית: React + Tailwind, בהשראת המבנה של nightlife-zone.com, שיועבר בסוף לפרויקט קיים ב-Lovable (ולכן חייב להתאים למוסכמות שלהם).

## Stack ומוסכמות (חובה, בגלל היעד ב-Lovable)
- React + TypeScript + Vite
- Tailwind CSS
- shadcn/ui לרכיבי UI (Badge, Button וכו')
- טוקני עיצוב כ-CSS variables (HSL) — לא צבעים קשיחים
- `lucide-react` לאייקונים
- alias יבוא `@/`
- **רספונסיביות מלאה (חובה מוחלטת):** מילוי מלא של ה-viewport בכל גודל מסך, בלי גלילה אופקית ובלי "letterbox bars". גריד הכרטיסיות עובר מ-3-4 עמודות בדסקטופ ל-2 בטאבלט ל-עמודה אחת במובייל. תמונות ואלמנטים עם צורה (לוגו, אייקונים) שומרים aspect-ratio ולעולם לא מתעוותים; קונטיינרים מבניים מתמתחים בחופשיות

## השראה עיצובית (מבוסס על 3 אתרי ייחוס שסופקו)
נבדקו hasfaniyot.net, gogogirls.co.il ו-nightlife-zone.com. דפוסים משותפים שכדאי לאמץ (מבחינת מבנה/UX, לא תוכן/טקסט - זה מגיע מהלקוח):
- **כרטיסיית חשפנית:** תמונה, תגית זמינות ("פנויה"), שם, תגיות ערים/אזורים כ-badges קטנים, שני כפתורי CTA ברורים (חיוג + וואטסאפ) בתחתית הכרטיסייה
- **Hero עם trust badges:** שורת אייקונים קצרה מתחת לכותרת הראשית (למשל: דיסקרטיות מלאה · זמינות 24/7) - תופסת מקום קטן אך מעבירה מסר מהיר
- **תגיות ערים כ-pills עם מספר** (אופציונלי): כל תגית עיר מוצגת עם מספר הפרופילים הזמינים בה, ומאפשרת קפיצה מהירה לסינון
- **כפתור וואטסאפ צף/Sticky** בתחתית המסך במובייל - נגישות מיידית מכל מקום באתר, לא רק מתוך הכרטיסיות
- **Grid רספונסיבי** לכרטיסיות: 3-4 עמודות בדסקטופ, יורד בהדרגה למובייל

## הדר, פוטר ותפריט
- **Header:** לוגו (קישור חזרה לדף הבית), תפריט ניווט, מספר הטלפון המרכזי גלוי לצד כפתור וואטסאפ, sticky בגלילה (נשאר קבוע למעלה)
- **תפריט ניווט:** מכיוון שהאתר הוא עמוד יחיד עם קטלוג דינמי (לא ריבוי דפים), הפריטים בתפריט הם קישורי גלילה פנימית (anchor links) לאזורים בדף - לדוגמה "הקטלוג שלנו", "אזורי שירות" - ובנוסף קישור רגיל לעמוד "תקנון אתר" (route נפרד). בגרסת מובייל: תפריט המבורגר שנפתח לרשימה
- **Footer:** מספר טלפון + כפתור וואטסאפ, קישור לעמוד התקנון, שורת זכויות יוצרים (© + שם העסק). אופציונלי: רשימת תגיות הערים כקישורי גלילה, בדומה למבנה שראינו באתרי הייחוס (עוזר גם ל-SEO פנימי בסיסי)

## מבנה הפרויקט
```
src/
├── components/
│   ├── GirlCard.tsx        # כרטיסיית חשפנית: תמונה, שם, תגיות, טלפון+וואטסאפ
│   ├── TagFilter.tsx        # רשימת תגיות עיר/אזור לסינון
│   ├── CatalogGrid.tsx      # גריד כרטיסיות מסונן
│   ├── Header.tsx           # לוגו, תפריט ניווט, מספר טלפון גלוי
│   ├── Footer.tsx           # פרטי יצירת קשר, קישור לתקנון, זכויות יוצרים
│   ├── StickyWhatsApp.tsx  # כפתור וואטסאפ צף, קבוע בתחתית המסך במובייל
│   └── admin/
│       ├── AdminLogin.tsx    # התחברות (email+password)
│       ├── AdminDashboard.tsx # רשימת כל החשפניות + כפתורי עריכה/מחיקה
│       └── GirlForm.tsx       # טופס הוספה/עריכה (שם, תיאור, תמונות, תגיות, מצב הצגה)
├── pages/
│   ├── Home.tsx              # קטלוג ראשי + סינון תגיות
│   ├── Terms.tsx               # עמוד תקנון אתר
│   └── Admin.tsx                # route מוגן, עוטף Dashboard/Form
├── lib/
│   └── supabase.ts               # חיבור ל-Supabase (client + auth)
├── data/
│   └── tags.ts                    # רשימת תגיות עיר/אזור (קבועה, לא דרך אדמין)
├── types/
│   └── index.ts                    # טיפוסי Girl, Tag
└── App.tsx
```

## Backend — Supabase
- **טבלה `girls`** במסד הנתונים (במקום קובץ נתונים סטטי) עם השדות שבמודל למטה
- **Storage Bucket** לתמונות החשפניות (העלאה מרובה לכל פרופיל), עם ניצול Image Transformations המובנה של Supabase לדחיסה/שינוי גודל אוטומטי - קריטי בהינתן 15–25 פרופילים עם כמה תמונות כל אחד, כדי שהאתר יטען מהר במובייל
- **Auth** בסיסי (email+password) להגנה על `/admin` — רק בעל העסק מחובר
- הקטלוג הציבורי (`Home.tsx`) קורא מהטבלה בזמן אמת — כל שינוי באדמין משתקף מיד באתר
- **Lazy loading** לתמונות בקטלוג (`loading="lazy"` או ספריית lazy-load) כדי לא לטעון את כל התמונות בבת אחת

## RLS (Row Level Security) — חובה, לא אופציונלי
בלי מדיניות RLS מוגדרת נכון, כל אחד עם גישת API יכול לקרוא/לערוך/למחוק נתונים ישירות מ-Supabase בלי לעבור דרך האדמין. חייבות להיות שתי מדיניות ברורות על טבלת `girls`:
- **קריאה (SELECT):** ציבורית, אך מוגבלת לרשומות עם `active = true` בלבד
- **כתיבה (INSERT/UPDATE/DELETE):** רק למשתמש מאומת (authenticated), לא לאף אחד אחר
זה חלק בלתי נפרד מהקמת ה-Backend, לא שיפור עתידי.

## פרטי יצירת קשר (קבוע לכל האתר, לא per-girl)
כל כפתורי הטלפון והוואטסאפ בכל הכרטיסיות מפנים לאותו מספר מרכזי - **054-914-0720** - בדיוק כמו בשני האתרים הקיימים של הלקוח. המספר מוגדר פעם אחת כקבוע/הגדרה גלובלית (`src/config/contact.ts` או משתנה סביבה), ולא כשדה בטופס האדמין לכל חשפנית.

כפתור הוואטסאפ בכל כרטיסייה כולל הודעה מוכנה מראש עם שם החשפנית, למשל:
`https://wa.me/972549140720?text=` + encodeURIComponent(`מעוניין ב${girl.name}`)
כך שהלקוח שלוחץ מהכרטיסייה מגיע לוואטסאפ עם הודעה שכבר מזהה על מי הוא שאל.

## מודל נתונים
```typescript
interface Girl {
  id: string;
  name: string;
  description: string;   // טקסט חופשי
  images: string[];        // URLs מ-Supabase Storage
  tags: string[];           // מזהי תגיות עיר/אזור
  active: boolean;           // הצג/הסתר בקטלוג בלי מחיקה
  order: number;               // סדר תצוגה בקטלוג
}

interface Tag {
  id: string;
  label: string;
  region?: string;        // קיבוץ תחת אזור (מרכז/דרום/שרון/ירושלים והסביבה)
}
```

## תגיות ערים/אזורים (מוגדר סופית)
- **מרכז:** רמת גן, ראשון לציון, תל אביב, חולון
- **שרון/חיפה:** נתניה, חדרה, אור עקיבא, חריש
- **דרום:** אשדוד, יבנה, אשקלון, אילת, שדות מיכה
- **ירושלים והסביבה:** ירושלים, מודיעין

## מה בפרויקט
- הדר עם לוגו, תפריט ניווט (גלילה פנימית + קישור לתקנון), וטלפון/וואטסאפ גלויים
- דף בית עם קטלוג דינמי מבוסס תגיות (15–25 כרטיסיות בנות)
- סינון לפי עיר/אזור — הכל באותו עמוד, בלי routing נפרד לכל עיר (state-based filtering, לא עמודים סטטיים)
- כל כרטיסייה: כפתור שיחת טלפון + כפתור WhatsApp
- כפתור וואטסאפ צף/Sticky למובייל
- עמוד תקנון אתר (route נפרד)
- פוטר עם פרטי יצירת קשר, קישור לתקנון וזכויות יוצרים
- מערכת ניהול אדמין (`/admin`, מוגנת בהתחברות) — הוספה, עריכה ומחיקה של חשפניות: שם, תיאור, תמונות, תגיות, מצב הצגה (פעיל/מוסתר)

זהו ההיקף המלא של הפרויקט - לא לייצר עמודים או מקטעים נוספים מעבר לרשימה הזו.

## תוכן
כל התוכן (לוגו, תמונות, טקסטים) מסופק על ידי הלקוח. לא לייצר תמונות/טקסט שיווקי סופי מבלי לוודא מול המפתח.

## SEO בסיסי לעמוד הבית
כותרת (Title) ותיאור מטא (Meta Description) בסיסיים לעמוד הבית עצמו - שורת קוד ב-`index.html`/`head`, לא אסטרטגיית תוכן. חיבור ל-MySEOPilot או קידום שוטף אינם חלק מהפרויקט הזה.

## הערה לגבי Lovable
זהו שלב הבנייה המקומי בלבד. הפרויקט יועבר בסוף ל-Lovable כשלב אחרון (integration), לא ייבנה משם מהתחלה. לכן חשוב לשמור על מוסכמות React+Tailwind+shadcn/ui מההתחלה כדי שההעברה תהיה חלקה. Lovable תומך באופן טבעי בחיבור Supabase, כך שהבחירה ב-Supabase כ-Backend צפויה להשתלב היטב בהעברה.
